name: Release

on:
  push:
    branches:
      - main
    tags:
      - 'v*'

jobs:
  # Version bump job - only runs on main branch
  version:
    if: github.ref == 'refs/heads/main'
    runs-on: ubuntu-latest
    outputs:
      new_version: ${{ steps.semver.outputs.new_version }}
    steps:
      - uses: actions/checkout@v4
        with:
          fetch-depth: 0

      - name: Bump version and push tag
        id: semver
        uses: anothrNick/github-tag-action@1.67.0
        env:
          GITHUB_TOKEN: ${{ secrets.GH_PAT }}
          WITH_V: true
          DEFAULT_BUMP: patch
          RELEASE_BRANCHES: main

  # Release job - runs on both main (after version bump) and on tag pushes
  goreleaser:
    needs: [version]
    if: github.ref == 'refs/heads/main' || startsWith(github.ref, 'refs/tags/v')
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4
        with:
          fetch-depth: 0

      - name: Set up Go
        uses: actions/setup-go@v5
        with:
          go-version: '1.21'

      - name: Run GoReleaser
        uses: goreleaser/goreleaser-action@v5
        with:
          distribution: goreleaser
          version: latest
          args: release --clean
        env:
          GITHUB_TOKEN: ${{ secrets.GH_PAT }}
          HOMEBREW_TAP_TOKEN: ${{ secrets.GH_PAT }}
          # Use either the bumped version or the existing tag
          HOMEBREW_BUMP_TAG: ${{ needs.version.outputs.new_version || github.ref_name }}

  # Homebrew job - only runs on tag pushes
  homebrew:
    if: startsWith(github.ref, 'refs/tags/v')
    needs: [goreleaser]
    runs-on: ubuntu-latest
    steps:
      - name: Update Homebrew formula
        uses: dawidd6/action-homebrew-bump-formula@v4
        with:
          token: ${{ secrets.GH_PAT }}
          tap: DevHatRo/homebrew-tap
          formula: tf-state-move
          tag: ${{ github.ref_name }}
          revision: ${{ github.sha }}
          force: true 
